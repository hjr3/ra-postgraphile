export declare const defaultQueryValueToInputValueMap: {
    GeometryPoint: (value: {
        geojson: {
            coordinates: number[];
        };
        type: string;
    }) => {
        coordinates: number[];
    };
    GeometryGeometry: (value: {
        geojson: {
            coordinates: number[];
        };
        type: string;
    }) => {
        coordinates: number[];
    };
    GeographyPoint: (value: {
        geojson: {
            coordinates: number[];
        };
        type: string;
    }) => {
        coordinates: number[];
    };
};
